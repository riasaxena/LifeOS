# Xcode
build/
DerivedData/
# The .xcodeproj is generated by XcodeGen from project.yml (see SETUP.md) -
# never committed, so it can't rot-diff across machines.
*.xcodeproj/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata/
*.xccheckout
*.moved-aside
*.xcuserstate
*.xcscmblueprint
*.xcworkspace/xcuserdata/

# Swift Package Manager
.swiftpm/
.build/

# CocoaPods (not used, but just in case)
Pods/

# macOS
.DS_Store

# JetBrains IDEs (IntelliJ, WebStorm, etc.)
.idea/

# Environment / secrets (e.g. future Oura API keys)
.env
Secrets.swift
